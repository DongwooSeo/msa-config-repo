# ~/config-repo/application.yml — "모든" Config Client 에게 공통으로 배달된다
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka
    refresh:
      enable: false   # Eureka 클라이언트를 refresh 대상에서 제외
